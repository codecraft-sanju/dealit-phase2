version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "5000:5000" 
    restart: always 

  frontend:
    build: ./frontend
    ports:
      - "5173:80"  # Bas yahan maine 80:80 ko hata kar 5173:80 kar diya hai
    depends_on:
      - backend 
    restart: always